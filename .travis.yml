language: generic
sudo: required
services:
  - docker

env:
  global:
    - SHA=$(git rev-parse HEAD)

before_script:
  - docker-compose build
  - docker-compose up -d

script:
  - docker ps -a

after_script:
  - docker-compose kill